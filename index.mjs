import generateRandomId from "./module.mjs";

generateRandomId();

/**
 * Alternative:
 * You can use ECMAScript modules with the .js file extension by adding "type": "module" to the package.json file.
 */
